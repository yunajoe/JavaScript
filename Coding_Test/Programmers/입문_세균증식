function solution(n, t) {
    var answer = new Array();
    let i = 0;
    while(i<=t-1){
        let result = n*2
        answer.push(result)
        n = result;
        i++
    }  // arr[arr.length - 1];
    return answer[answer.length -1];
  
}
