
function solution(before, after) {
  let answer;
  let before_arr = [...before];
  let reverseBeforeArr = before_arr.reverse(); // object
  let after_arr = [...after];

  if (after_arr.toString() === reverseBeforeArr.toString()) {
    answer = 1;
  } else {
    answer = 0;
  }
  return answer;
}


정확성: 65.2
합계: 65.2 / 100.0
